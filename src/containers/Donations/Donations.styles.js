import styled from "@emotion/styled";

export const DonationsStyles = styled.div(({ theme }) => ({
  color: theme.colors.white,
  paddingTop: 100,
  height: "100%",

  display: "flex",
  flexDirection: "column",
  justifyContent: "center",
  alignItems: "center",
}));
